import { DirectivesDirective } from './stylus.directive';

describe('DirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
